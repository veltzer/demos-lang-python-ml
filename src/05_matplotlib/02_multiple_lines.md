# Exercise 03: Multiple Lines on One Axes

On a single set of axes, plot three curves of `x` from `0` to `2*pi` (use `np.linspace`
with 100 points):

- `sin(x)` labeled `"sin"`
- `cos(x)` labeled `"cos"`
- `sin(x) * cos(x)` labeled `"sin*cos"`

Add a legend, an x-axis label `"x"`, a y-axis label `"value"`, and save to
`/tmp/03_multiple_lines.png`.
