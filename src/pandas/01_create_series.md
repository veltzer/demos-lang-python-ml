# Exercise 01: Create a Series

Create a pandas `Series` containing the values `10, 20, 30, 40, 50` with the index
labels `"a", "b", "c", "d", "e"`.

Print:

1. the series itself
2. its `dtype`
3. its `values` (as a NumPy array)
4. its `index`
