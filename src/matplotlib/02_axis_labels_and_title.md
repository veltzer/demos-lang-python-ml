# Exercise 02: Axis Labels, Title, and Legend

Plot `y = x**2` for `x` in `0..9` and annotate the figure:

1. Set the x-axis label to `"x"`.
2. Set the y-axis label to `"y = x^2"`.
3. Set the title to `"Square function"`.
4. Pass `label="x^2"` to `plt.plot` and call `plt.legend()` to display it.
5. Save to `/tmp/02_axis_labels_and_title.png`.
