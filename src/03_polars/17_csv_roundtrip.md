# Exercise 18: CSV Round-Trip

Take a `DataFrame`, write it to CSV (into an in-memory buffer), print the CSV
text, then read it back into a new `DataFrame`. Confirm the round-tripped frame
equals the original.
