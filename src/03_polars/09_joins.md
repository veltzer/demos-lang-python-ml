# Exercise 10: Joins

Given an `employees` table and a `departments` table sharing a `dept_id` key:

1. inner-join employees to their department
2. left-join employees to their department
3. use an anti-join to find departments that have no employees
