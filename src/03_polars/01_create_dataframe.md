# Exercise 02: Create a DataFrame

Build a polars `DataFrame` from a dict of columns holding the `name`, `age`, and
`salary` of five people.

Print:

1. the dataframe itself
2. its `shape`
3. its `columns`
