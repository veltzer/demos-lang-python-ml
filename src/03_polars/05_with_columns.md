# Exercise 06: Derive Columns

Use `with_columns` to add three derived columns to a `DataFrame`:

1. a `raised` column: salary increased by 10% and rounded
2. an `upper_name` column: the name upper-cased
3. a `well_paid` boolean column: `True` when salary exceeds 60000
