# Exercise 12: String Expressions

Given a `DataFrame` of email addresses, use the `str` expression namespace to add:

1. a `normalized` column: the email lower-cased
2. a `user` column: the part before the `@`
3. a `length` column: the character length
4. an `is_example` column: whether it contains `example` (case-insensitive)
