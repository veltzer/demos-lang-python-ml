# Exercise 11: Lazy Frames

Build a `LazyFrame` and chain a query that filters, groups by `dept`, and
computes the average salary.

1. print the optimized query plan with `explain`
2. materialize the result with `collect`
