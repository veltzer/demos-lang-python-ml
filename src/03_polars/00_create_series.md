# Exercise 01: Create a Series

Create a polars `Series` named `"nums"` containing the values `10, 20, 30, 40, 50`.

Print:

1. the series itself
2. its `dtype`
3. its `name`
4. its values as a plain Python list
