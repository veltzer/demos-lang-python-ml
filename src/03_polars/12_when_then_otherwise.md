# Exercise 13: When / Then / Otherwise

Given a `DataFrame` of test scores, add a `grade` column using a
`when / then / otherwise` chain:

- `A` for scores 90 and above
- `B` for scores 70 to 89
- `C` for everything else
