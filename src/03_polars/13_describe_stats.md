# Exercise 14: Summary Statistics

Given a numeric `DataFrame`:

1. print the built-in `describe` summary
2. compute the mean, standard deviation, median, and 75th percentile of the
   `salary` column with explicit expressions
