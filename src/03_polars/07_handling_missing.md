# Exercise 08: Handling Missing Values

Given a `DataFrame` with some `null` values:

1. count the nulls per column
2. drop every row containing a null
3. fill the `age` nulls with the column mean and the `salary` nulls with `0`
