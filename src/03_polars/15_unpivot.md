# Exercise 16: Unpivot

Given a wide table with a `dept` column and one column per quarter, unpivot
(melt) it back into long form with `quarter` and `revenue` columns. Sort the
result by department and quarter.
