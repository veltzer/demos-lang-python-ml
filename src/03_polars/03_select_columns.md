# Exercise 04: Select Columns

Given a `DataFrame` of people, show several ways to select data:

1. select the `name` and `salary` columns by name
2. select the `age` and `salary` columns using `pl.col(...)`
3. take the first two rows with `head`
4. extract a single column as a `Series`
