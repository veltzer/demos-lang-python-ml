# Exercise 09: Group By

Given a `DataFrame` with a `dept` column, group by department and compute per
department:

1. the head count
2. the average salary
3. the maximum salary

Sort the result by department.
