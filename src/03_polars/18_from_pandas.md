# Exercise 19: Converting To And From pandas

Starting from a pandas `DataFrame`:

1. convert it to a polars `DataFrame` with `pl.from_pandas`
2. convert it back to pandas with `to_pandas`

Print each result and its type to confirm the conversions.
