# Exercise 07: Sorting

Given a `DataFrame` with `dept` and `salary` columns, sort it:

1. by `salary` ascending
2. by `salary` descending
3. by `dept` ascending, then `salary` descending within each department
