# Exercise 02: Array Arithmetic

Given two arrays `a = [1, 2, 3, 4]` and `b = [10, 20, 30, 40]`, compute and print:

1. their element-wise sum
2. their element-wise product
3. `b - a`
4. `a` squared (each element squared)

Use NumPy operations — no Python loops.
