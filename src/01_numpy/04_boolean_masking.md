# Exercise 05: Boolean Masking

Given `a = np.array([3, -1, 4, -1, 5, 9, -2, 6, 5, 3, 5])`:

1. Print all positive elements.
2. Replace every negative element with `0` and print the result.
3. Print the count of elements greater than `4`.

Use boolean masks — no Python loops.
