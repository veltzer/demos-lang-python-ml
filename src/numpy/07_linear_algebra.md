# Exercise 08: Linear Algebra

Solve the linear system `A x = b` where:

```
A = [[3, 1],
     [1, 2]]

b = [9, 8]
```

1. Solve for `x` using `np.linalg.solve`.
2. Verify by printing `A @ x` and confirming it equals `b`.
3. Print the determinant of `A` and its inverse.
